export function getName() {
  return new Promise((resolve) => {setTimeout(resolve.bind(null, 'Data Tables'), 1000)})
} 

export function getData() {
  const data = [];
  
  return new Promise((resolve) => {

  })
}